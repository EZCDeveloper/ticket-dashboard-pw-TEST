import { test } from '../../fixtures/base.fixtures';

test.describe('1. CRUD Tickets', () => {

   test('TC-1.1. Create a Ticket Successfully', async ({ basePage }) => {
      await basePage.navigateTo("/")
   })
})